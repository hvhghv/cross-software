#!/bin/sh

action=${1:-stop}
case "$action" in
  stop)
    sync
    swapoff -a 2>/dev/null || true
    umount -a -r 2>/dev/null || true
    ;;
  *)
    echo "Usage: $0 [stop]" >&2
    exit 2
    ;;
esac
